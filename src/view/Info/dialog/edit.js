import React, { memo } from 'react';
import PropTypes from 'prop-types';

const Edit = memo((props) => {
    return (
        <div>
            
        </div>
    );
});

Edit.propTypes = {
    
};

export default Edit;